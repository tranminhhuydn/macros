console.clear();
//console.log("test.js a ha")
  // const fs = require("fs")
  // var scriptDir = this.path.parse(__dirname)
  // scriptDir = path.join(scriptDir.dir, 'data')
  // var dataDir = []
  // //dataDir
  // function fnDir (items) {
  //   dataDir = items;
  // }
  // dataDir = fs.readdirSync(scriptDir)
  // var data = {}
  // for (var i = 0; i < dataDir.length; i++) {
  //   var _name = dataDir[i]
  //   if(_name =="App.json")
  //   continue;
  //   var keyName = _name.replace(/\s/g,"_").replace(".json","")
  //   //console.log(keyName+"=>"+_name);
  //   if(keyName.indexOf(".js")==-1)
  //   data[keyName] =  require("../data/"+_name).data;
  // }
  // console.log(data);

  console.log(this.listDB);
